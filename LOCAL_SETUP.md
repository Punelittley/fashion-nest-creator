# Инструкция по локальной разработке

## Скачивание проекта

1. **Клонируйте репозиторий на свой компьютер:**
   ```bash
   git clone <ваш-github-repo-url>
   cd <название-проекта>
   ```

## Установка зависимостей

### Backend (Node.js + Express + SQLite)

```bash
cd server
npm install
```

### Frontend (React + Vite)

```bash
# В корневой папке проекта
npm install
```

## Настройка окружения

### Backend

Создайте файл `server/.env`:

```bash
cd server
cp .env.example .env
```

Отредактируйте `server/.env`:
```env
PORT=3001
JWT_SECRET=ваш-секретный-ключ-измените-это
NODE_ENV=development
FRONTEND_URL=http://localhost:8080
```

## Запуск проекта

### Вариант 1: Два терминала

**Терминал 1 - Backend:**
```bash
cd server
npm start
```

Вы должны увидеть:
```
╔════════════════════════════════════════╗
║   Fashion Store Backend Server         ║
╠════════════════════════════════════════╣
║   🚀 Server: http://localhost:3001     ║
║   📊 API: http://localhost:3001/api    ║
╚════════════════════════════════════════╝
✓ Подключение к SQLite базе данных установлено
✓ База данных инициализирована
```

**Терминал 2 - Frontend:**
```bash
# В корневой папке
npm run dev
```

Откройте браузер: http://localhost:8080

### Вариант 2: Для разработки с автоперезагрузкой

Backend с автоперезагрузкой:
```bash
cd server
npm run dev
```

## Тестовые данные

После первого запуска автоматически создаются:

**Администратор:**
- Email: admin@test.com
- Пароль: admin123

**Категории:**
- Мужское
- Женское
- Аксессуары

**Тестовые товары:** несколько товаров в каждой категории

## База данных

SQLite база автоматически создается в `server/fashion_store.db`

Для просмотра базы можете использовать:
- [DB Browser for SQLite](https://sqlitebrowser.org/)
- [SQLite Viewer](https://inloop.github.io/sqlite-viewer/)
- VS Code расширения для SQLite

## Структура API

Backend доступен на `http://localhost:3001/api`:

### Аутентификация
- `POST /api/auth/signup` - Регистрация
- `POST /api/auth/signin` - Вход
- `GET /api/auth/me` - Текущий пользователь
- `POST /api/auth/signout` - Выход

### Товары
- `GET /api/products` - Список товаров
- `GET /api/products/:id` - Товар по ID
- `POST /api/products` - Создать (админ)
- `PUT /api/products/:id` - Обновить (админ)
- `DELETE /api/products/:id` - Удалить (админ)

### Категории
- `GET /api/categories` - Список категорий
- `POST /api/categories` - Создать (админ)

### Корзина
- `GET /api/cart` - Получить корзину
- `POST /api/cart` - Добавить товар
- `PUT /api/cart/:id` - Обновить количество
- `DELETE /api/cart/:id` - Удалить товар
- `DELETE /api/cart` - Очистить корзину

### Заказы
- `GET /api/orders` - Заказы пользователя
- `POST /api/orders` - Создать заказ
- `GET /api/orders/all` - Все заказы (админ)
- `PATCH /api/orders/:id/status` - Обновить статус (админ)

### Профиль
- `GET /api/profile` - Получить профиль
- `PUT /api/profile` - Обновить профиль

## Устранение проблем

### Backend не запускается

1. Проверьте, что Node.js установлен:
   ```bash
   node --version  # должно быть >= 16.x
   ```

2. Проверьте, что порт 3001 свободен:
   ```bash
   # Windows
   netstat -ano | findstr :3001
   
   # Mac/Linux
   lsof -i :3001
   ```

3. Переустановите зависимости:
   ```bash
   cd server
   rm -rf node_modules
   npm install
   ```

### Frontend не подключается к Backend

1. Убедитесь, что backend запущен на порту 3001
2. Проверьте `src/lib/api.ts` - должно быть `http://localhost:3001/api`
3. Откройте консоль браузера (F12) и проверьте ошибки

### База данных не инициализируется

Удалите файл базы и перезапустите:
```bash
cd server
rm fashion_store.db
npm start
```

## Дальнейшая разработка

После внесения изменений в код:
- Frontend: автоматически перезагружается (hot reload)
- Backend: перезагрузится автоматически если используете `npm run dev`
