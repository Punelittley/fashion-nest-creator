# 🎯 Полноценная локальная разработка

## Запуск

**Терминал 1 (Backend SQLite):**
```bash
cd server
npm install
npm start
```

**Терминал 2 (Frontend):**
```bash
npm install
npm run dev
```

Откройте: **http://localhost:8080**

---

## Что работает?

✅ **Админ-панель** — добавление/редактирование товаров через интерфейс → сохраняется в SQLite  
✅ **Редактирование кода** — изменения в `server/init-db.sql` → видны после пересоздания БД  
✅ **Каталог** — показывает товары из SQLite  
✅ **Корзина, заказы** — работают с SQLite  

---

## Редактирование через код (init-db.sql)

### 1. Отредактируйте файл
Откройте `server/init-db.sql` и измените товар:

```sql
INSERT OR IGNORE INTO products (id, category_id, name, description, price, image_url, stock, is_active) VALUES 
('prod-004', 'cat-002', 'Новое название', 'Новое описание', 999, '/images/pants/pants1.jpg', 10, 1);
```

### 2. Пересоздайте базу данных

**Windows:**
```bash
cd server
Remove-Item fashion_store.db -Force
npm start
```

**Mac/Linux:**
```bash
cd server
rm fashion_store.db
npm start
```

### 3. Обновите страницу
Изменения сразу видны в каталоге и админ-панели.

---

## Редактирование через админ-панель

1. Откройте http://localhost:8080/admin
2. Войдите: `qwe@gmail.com` / любой пароль (auth через Supabase, но товары в SQLite)
3. Добавляйте/редактируйте товары — всё сохраняется в SQLite
4. Изменения сразу видны в каталоге

---

## Смена картинок товаров

### Вариант 1: Локальные файлы (для SQLite)

1. Положите картинку в `public/images/` по категориям:
   - `public/images/coats/` — верхняя одежда
   - `public/images/pants/` — штаны
   - `public/images/scarves/` — шарфы
   - `public/images/shoes/` — обувь

2. В `init-db.sql` укажите путь:
```sql
image_url: '/images/coats/my-coat.jpg'
```

3. Пересоздайте БД (см. выше)

### Вариант 2: Загрузка через админку

1. В админ-панели нажмите "Загрузить изображение"
2. Файл загрузится в Supabase Storage
3. Но это работает только при использовании Supabase (без бекенда)

---

## Структура категорий

SQLite использует ID: `cat-001`, `cat-002`, `cat-003`, `cat-004`

```sql
'cat-001' → 'Верхняя одежда'
'cat-002' → 'Штаны и брюки'
'cat-003' → 'Шарфы'
'cat-004' → 'Обувь'
```

---

## Важно!

⚠️ **Два режима работы:**

**Режим 1: Оба терминала** → SQLite (локальная БД)  
- Админ-панель → SQLite
- Каталог → SQLite
- Изменения в init-db.sql работают

**Режим 2: Только фронтенд** → Supabase (облако)  
- Админ-панель → Supabase
- Каталог → Supabase
- Синхронизация с preview

**НЕ СМЕШИВАЙТЕ!** Если работаете с SQLite — используйте оба терминала. Если с Supabase — только фронтенд.

---

## Проверка что всё работает

1. ✅ Добавьте товар через админку → он появился в каталоге
2. ✅ Измените товар в init-db.sql → пересоздайте БД → изменения видны
3. ✅ Картинки из `public/images/` отображаются

Всё работает? 🎉
