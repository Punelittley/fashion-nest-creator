# 🚀 Полноценная работа: SQLite + Админ-панель

Теперь можно запускать фронтенд + бекенд и редактировать `init-db.sql` с работающей админ-панелью!

## Как это работает?

Админ-панель теперь работает по той же схеме что и каталог:
1. Сначала пробует SQLite (если бекенд запущен)
2. Если не получается — автоматически переключается на Supabase

## Запуск

### Терминал 1 (Бекенд):
```bash
cd server
npm install
npm start
```

### Терминал 2 (Фронтенд):
```bash
npm install
npm run dev
```

### Откройте: http://localhost:8080

## Что теперь работает? ✅

✅ **Каталог** — показывает товары из SQLite  
✅ **Админ-панель** — редактирует товары в SQLite  
✅ **Корзина** — работает с SQLite  
✅ **Заказы** — работают с SQLite  
✅ **Редактирование init-db.sql** — применяется сразу после пересоздания БД

## Как редактировать товары через init-db.sql?

1. Отредактируйте `server/init-db.sql`
2. Удалите старую базу: `rm server/fashion_store.db`
3. Перезапустите бекенд: `npm start` (в папке `server`)
4. Обновите страницу в браузере

Изменения сразу видны в каталоге и админ-панели!

## Пример: изменить товар

```sql
-- В server/init-db.sql
INSERT OR IGNORE INTO products (id, category_id, name, description, price, image_url, stock, is_active) VALUES 
('prod-004', 'cat-002', 'Я помню', 'Строгие брюки', 666, '/images/pants/pants1.jpg', 15, 1);
```

Затем:
```bash
rm server/fashion_store.db
cd server && npm start
```

## Когда использовать Supabase?

Если запустите **только фронтенд** (`npm run dev` без бекенда):
- Все автоматически переключится на Supabase
- Админ-панель будет работать с облачной БД
- Изменения синхронизируются с preview

## Итого

🎯 **Локальная разработка:** Фронтенд + Бекенд (SQLite) + редактирование init-db.sql  
☁️ **Облачная разработка:** Только фронтенд (Supabase) + админ-панель  

Можете выбирать любой вариант в зависимости от задачи!
