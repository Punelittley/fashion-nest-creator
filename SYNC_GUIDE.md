# 🔄 Синхронизация init-db.sql → Supabase

## Как это работает?

Теперь вы можете редактировать `server/init-db.sql` и применять изменения в Supabase (для админ-панели).

## Использование

### 1. Отредактируйте init-db.sql

Измените товары в файле `server/init-db.sql`:

```sql
INSERT OR IGNORE INTO products (id, category_id, name, description, price, image_url, stock, is_active) VALUES 
('prod-004', 'cat-002', 'Я помню', 'Строгие брюки прямого кроя', 666, '/images/pants/pants1.jpg', 15, 1);
```

### 2. Запустите синхронизацию

```bash
node scripts/sync-products.js
```

### 3. Проверьте результат

- Откройте админ-панель: http://localhost:8080/admin
- Проверьте каталог: http://localhost:8080/catalog
- Изменения будут видны в preview тоже

## Что синхронизируется?

✅ Название товара  
✅ Описание  
✅ Цена  
✅ Остаток (stock)  
✅ Картинка  
✅ Категория  
✅ Активность (is_active)

## Требования

Скрипт требует переменные окружения в `.env` (они уже есть):
- `VITE_SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

## Важно

⚠️ Скрипт **НЕ удаляет** товары из Supabase  
⚠️ Если товар с таким именем существует - он обновится  
⚠️ Если товара нет - он создастся  

## Workflow

```
1. Редактируете init-db.sql
2. Запускаете node scripts/sync-products.js
3. Проверяете в админ-панели
4. Готово! ✅
```
